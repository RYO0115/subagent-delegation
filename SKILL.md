---
name: subagent-delegation
description: >-
  Fable / Opus など高価なモデルで作業しているとき、Sonnet / Haiku で十分こなせる
  単純・大量・機械的な作業を subagent(fresh general-purpose、model 上書き)に委譲して
  親コンテキストのトークン消費を抑えるスキル。委譲先が完了したら親(Fable/Opus)が
  成果をレビューし、修正・改善が要る箇所を洗い出して同じ subagent に再委譲する。
  目的は使用トークンの削減。ただし cold-start コストを踏まえ「委譲するとかえって高くつく」
  小さな作業は委譲しない判断も含む。
  「これ subagent にやらせて」「安いモデルに投げて」「トークン節約して委譲」で起動。
  一括編集・広域探索・定型生成・長いログ/ファイルの要約など単純作業を始める前に検討する。
---

# 安いモデルへの委譲でトークンを削る(subagent-delegation)

Fable / Opus は賢いが高い。**賢さが要らない作業**(大量の機械編集・広域の探索・定型生成・
長文の読解要約)まで高価なモデルの文脈で回すのは無駄。そこを Sonnet / Haiku の subagent に
出し、親は**指示とレビューだけ**を担う。これがこのスキルの全体像。
着手前に難易度から実行モードを決める [task-triage](../task-triage/SKILL.md) の
「重」クラスから接続されるのが典型的な入口。

## トークンが減る理屈(これを外すと逆に増える)

トークンが減る本当の理由は「安いモデルだから」だけではない。**subagent のツール出力
(読んだファイル全文・検索ヒット・試行錯誤のログ)が親コンテキストに入らない**からだ。
親には subagent の**最終報告だけ**が返る。だから:

- **委譲が得なのは**「中間出力が大量で、結論/成果物は小さい」作業。例:20ファイルを読んで
  1つの表にまとめる、広いツリーを探索して該当箇所だけ返す、大量の定型置換をして diff だけ返す。
- **委譲が損なのは**「中間出力が小さい/文脈の受け渡しが高くつく」作業。fresh subagent は
  **cold start**(このリポジトリの規約・文脈をゼロから渡し直す)なので、数ファイルの小改修や
  一問一答は、ブリーフを書く手間+再説明のほうが高い。**自分でやったほうが安い。**

判断の詳細は [references/when-to-delegate.md](references/when-to-delegate.md)(委譲する/しないの
ROI 基準と Sonnet/Haiku の選び分け)。

## 適用条件

- 親が **Fable または Opus** で動いていて、これから**単純作業**に入るとき。
  (親が既に Sonnet/Haiku なら下へ委譲しても節約にならない — 自分でやる。)
- **自分が subagent(fork 含む)側なら委譲しない** — その場で実行する(多段委譲は無駄)。
- ユーザーがこのスキル/委譲を望んだとき、または明らかに委譲が得な単純作業を自分で始めようと
  したとき。ユーザーが個別に「これをやって」と言った一回きりの小作業に、勝手に多数の
  subagent を撒かない(節約が目的であって委譲自体が目的ではない)。

## 手順

### Step 1 — 委譲するか決める(しない判断も成果)

[references/when-to-delegate.md](references/when-to-delegate.md) の ROI テストに通す。
通らなければ**自分でやる**。無理に委譲しない。委譲すると決めたら:

- 作業を **1つのよく絞られた依頼**にまとめる(関連する機械作業はまとめて1回で出す。
  同種の作業に subagent を何体も並べない — briefing の重複コストで元が取れない)。
- モデルを選ぶ:**Haiku=純粋に機械的**(定型置換・整形・単純抽出)、**Sonnet=少し判断が要る**
  (コードのリファクタ・仕様からの実装・要約の取捨選択)。迷ったら Sonnet。
- 読み取り専用の広域探索は `subagent_type: "Explore"`、生成/編集を伴うなら
  `subagent_type: "general-purpose"`。いずれも `model` を上書きする。
  **`subagent_type: "fork"` は model 上書きが無視され親モデルで走る**ので使わない。

### Step 2 — ブリーフを書いて委譲する

レビューを安くする鍵は**受け入れ基準(acceptance criteria)を先に固定する**こと。
[references/brief-and-review.md](references/brief-and-review.md) のテンプレに沿って、
subagent への prompt に必ず入れる:

1. **ゴール**(何を作る/変える)と**対象範囲**(触るファイル/ディレクトリを明示、
   触ってはいけない所も明示)。
2. **このリポジトリの前提**(cold start 対策):関連する規約ファイルのパス
   (例 `CLAUDE.md`・該当 skill・`knowledge/voice.md` 等)を「まず読め」と渡す。全文を貼らずパスで渡す
   (subagent が自分で読む=その読解トークンは親に返らない)。
3. **受け入れ基準**(箇条書き・検証可能な形)。可能なら**機械チェックのコマンド**も渡し
   「これが通るまで直してから報告しろ」と指示(例:このリポジトリなら
   `uv run --project tools/prepublish-checks python tools/prepublish-checks/note_gate.py <draft>`、
   Python なら該当 pytest、リンク確認なら check_links.py)。
4. **報告フォーマット**:変更したファイル一覧+要点+**自己申告した未解決/不確実点**を
   簡潔に返させる(全文やログを貼らせない=親の節約)。

### Step 3 — 親(Fable/Opus)がレビューする ※このスキルの肝

**subagent の自己申告=完了ではない。** 親が独立にレビューする。ただし**レビュー自体を安く**
やる(全部読み直したら節約が消える):

- **成果物(diff・生成物・報告)だけを見る**。元資料を丸ごと読み直さない。
  `git diff` / 対象ファイルの該当箇所 / 機械チェックの再実行、で確認する。
- [references/brief-and-review.md](references/brief-and-review.md) のレビュー観点で照合:
  受け入れ基準の全項目を満たすか/範囲外を触っていないか/破綻・捏造・規約違反はないか/
  「できました」の裏が取れているか(機械チェックを**親が**1回回す)。
- 判断が要る所(このリポジトリなら voice・事実の真正性など)は既存の専用スキルに委ねる。
  例:記事なら [note-reviewer] を使う。車輪を再発明しない。

### Step 4 — 修正が要れば再委譲する(SendMessage で同じ subagent へ)

レビューで見つかった修正・改善は、**新しい subagent を立てず** `SendMessage` で
**同じ subagent に差し戻す**(その subagent は文脈が温まっているので cold start を再度払わずに済む)。

- 差し戻しは**具体的な修正リスト**で渡す(「◯行目の△を□に」「受け入れ基準3が未達、理由は…」)。
  曖昧な「もう一度見直して」は往復を増やしトークンを食う。
- **往復は原則2回まで**。2回直して基準に届かないなら、(a)親が自分で仕上げる、
  (b)ブリーフの設計ミスを疑い作り直す、(c)タスクが実は「判断が要る」= 委譲不適だったと見て
  引き取る。ズルズル往復させない(往復のたびにトークンを使う=目的に反する)。
- 全基準を満たしたらレビュー合格。ユーザーに、何を委譲し・どのモデルで・何を直させたかを
  1〜2行で報告する。

## やらないこと(節約を殺すアンチパターン)

- **cold start で損する小作業の委譲**(数ファイルの小改修・一問一答)。自分でやる。
- **同種作業への subagent 乱立**。まとめて1体に出す。
- **受け入れ基準なしの丸投げ** → レビューが高くつき、往復が増える。
- **成果を全文読み直すレビュー**。diff と機械チェックで済ませる。
- **判断・センスが要る中核**(著者の声・事実の断定・設計の意思決定)を安いモデルに委ねること。
  ここは親が持つ。委譲するのは「賢さの要らない手数」だけ。
